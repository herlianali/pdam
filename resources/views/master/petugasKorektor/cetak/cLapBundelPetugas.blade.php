hi laporan
@foreach ($tampil as $item)
    {{ $tampil->thbl }}||
    {{ $tampil->zona }}||
    {{ $tampil->no_bundel }}||
    {{ $tampil->jml_no_plg }}||
    {{ $tampil->potensial }}||
    {{ $tampil->koreksi }}||<br/>
@endforeach

